<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: cmd | 吴文洁]]></title>
  <link href="http://www.wuwenjie.tk/blog/categories/cmd/atom.xml" rel="self"/>
  <link href="http://www.wuwenjie.tk/"/>
  <updated>2014-12-28T17:16:27+08:00</updated>
  <id>http://www.wuwenjie.tk/</id>
  <author>
    <name><![CDATA[wuwenjie]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[[转]mysql cmd命令 MySQLCC++数据结构SQL]]></title>
    <link href="http://www.wuwenjie.tk/blog/2011/07/03/mysql-cmd-command-MySQL-C-data-structures-SQL/"/>
    <updated>2011-07-03T02:14:00+08:00</updated>
    <id>http://www.wuwenjie.tk/blog/2011/07/03/mysql-cmd-command-MySQL-C-data-structures-SQL</id>
    <content type="html"><![CDATA[<p><a href="http://www.wuwenjie.tk/wp-content/uploads/2011/07/start_mysql.gif"><img class="size-full wp-image-72" title="start_mysql" src="http://www.wuwenjie.tk/wp-content/uploads/2011/07/start_mysql.gif" alt="start_mysql" width="498" height="234" /></a></p>

<p>连接：mysql -h主机地址 -u用户名 －p用户密码 （注:u与root可以不用加空格，其它也一样）</p>

<p>断开：exit （回车）</p>

<p>创建授权：grant select on 数据库.* to 用户名@登录主机 identified by \&ldquo;密码\&rdquo;</p>

<p>修改密码：mysqladmin -u用户名 -p旧密码 password 新密码</p>

<p>删除授权: revoke select,insert,update,delete om <em>.</em> from test2@localhost;</p>

<p>显示数据库：show databases;</p>

<p>显示数据表：show tables;</p>

<p>显示表结构：describe 表名;</p>

<p>创建库：create database 库名;</p>

<p>删除库：drop database 库名;</p>

<p>使用库：use 库名;</p>

<p>创建表：create table 表名 (字段设定列表);</p>

<p>删除表：drop table 表名;</p>

<p>修改表：alter table t1 rename t2</p>

<p>查询表：select * from 表名;</p>

<p>清空表：delete from 表名;</p>

<p>备份表: mysqlbinmysqldump -h(ip) -uroot -p(password) databasename tablename &gt; tablename.sql</p>

<p>恢复表: mysqlbinmysql -h(ip) -uroot -p(password) databasename tablename &lt; tablename.sql（操作前先把原来表删除） 增加列：ALTER TABLE t2 ADD c INT UNSIGNED NOT NULL AUTO_INCREMENT,ADD INDEX &copy;; 修改列：ALTER TABLE t2 MODIFY a TINYINT NOT NULL, CHANGE b c CHAR(20); 删除列：ALTER TABLE t2 DROP COLUMN c; 备份数据库：mysql\bin\mysqldump -h(ip) -uroot -p(password) databasename &gt; database.sql</p>

<p>恢复数据库：mysql\bin\mysql -h(ip) -uroot -p(password) databasename &lt; database.sql 复制数据库：mysql\bin\mysqldump &mdash;all-databases &gt; all-databases.sql</p>

<p>修复数据库：mysqlcheck -A -o -uroot -p54safer</p>

<p>文本数据导入： load data local infile \&ldquo;文件名\&rdquo; into table 表名;</p>

<p>数据导入导出：mysql\bin\mysqlimport database tables.txt</p>

<p>执行sql文件：source 路径+文件（用“/”）2009-07-21</p>

<p>mysql cmd命令</p>

<p>MySQLCC++数据结构SQL</p>

<p>连接：mysql -h主机地址 -u用户名 －p用户密码 （注:u与root可以不用加空格，其它也一样）</p>

<p>断开：exit （回车）</p>

<p>创建授权：grant select on 数据库.* to 用户名@登录主机 identified by \&ldquo;密码\&rdquo;</p>

<p>修改密码：mysqladmin -u用户名 -p旧密码 password 新密码</p>

<p>删除授权: revoke select,insert,update,delete om <em>.</em> from test2@localhost;</p>

<p>显示数据库：show databases;</p>

<p>显示数据表：show tables;</p>

<p>显示表结构：describe 表名;</p>

<p>创建库：create database 库名;</p>

<p>删除库：drop database 库名;</p>

<p>使用库：use 库名;</p>

<p>创建表：create table 表名 (字段设定列表);</p>

<p>删除表：drop table 表名;</p>

<p>修改表：alter table t1 rename t2</p>

<p>查询表：select * from 表名;</p>

<p>清空表：delete from 表名;</p>

<p>备份表: mysqlbinmysqldump -h(ip) -uroot -p(password) databasename tablename &gt; tablename.sql</p>

<p>恢复表: mysqlbinmysql -h(ip) -uroot -p(password) databasename tablename &lt; tablename.sql（操作前先把原来表删除） 增加列：ALTER TABLE t2 ADD c INT UNSIGNED NOT NULL AUTO_INCREMENT,ADD INDEX &copy;; 修改列：ALTER TABLE t2 MODIFY a TINYINT NOT NULL, CHANGE b c CHAR(20); 删除列：ALTER TABLE t2 DROP COLUMN c; 备份数据库：mysql\bin\mysqldump -h(ip) -uroot -p(password) databasename &gt; database.sql</p>

<p>恢复数据库：mysql\bin\mysql -h(ip) -uroot -p(password) databasename &lt; database.sql 复制数据库：mysql\bin\mysqldump &mdash;all-databases &gt; all-databases.sql</p>

<p>修复数据库：mysqlcheck -A -o -uroot -p54safer</p>

<p>文本数据导入： load data local infile \&ldquo;文件名\&rdquo; into table 表名;</p>

<p>数据导入导出：mysql\bin\mysqlimport database tables.txt</p>

<p>执行sql文件：source 路径+文件（用“/”）</p>
]]></content>
  </entry>
  
</feed>
